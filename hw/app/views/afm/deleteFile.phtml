<?php

$post =  json_decode(file_get_contents('php://input'), true);

$fullname = BASE_PATH . "/../afm/" . $post["filename"] ;


if (is_dir($fullname)) {
    $result = rmdir($fullname);
} else {
    $result = unlink($fullname);
}

echo json_encode($result);
